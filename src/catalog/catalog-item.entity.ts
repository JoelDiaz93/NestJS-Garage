import { Column, Entity, PrimaryGeneratedColumn } from 'typeorm'; import { CatalogItemType } from '../common/enums';
@Entity('catalog_items') export class CatalogItem { @PrimaryGeneratedColumn('uuid') id:string; @Column({ unique:true }) sku:string; @Column() name:string; @Column({type:'enum',enum:CatalogItemType}) type:CatalogItemType; @Column('numeric',{precision:12,scale:2}) price:number; @Column('numeric',{precision:12,scale:2,default:0}) cost:number; @Column('int',{default:0}) stock:number; @Column('int',{default:0}) minStock:number; @Column({default:true}) active:boolean; @Column({nullable:true}) description?:string; }
